{
  "collectionName": "components_section_book_calls",
  "info": {
    "displayName": "Book Call",
    "description": "Editorial copy for the booking funnel. Duration, timezone, location, qualification and scheduling rules are managed in Injaaz Cal."
  },
  "options": {},
  "attributes": {
    "introEyebrow": {
      "type": "string",
      "default": "Injaaz Digital",
      "description": "Small brand label shown above the page title."
    },
    "pageTitle": {
      "type": "string",
      "required": true,
      "default": "Book a Strategy Call",
      "description": "Main title shown in the booking panel."
    },
    "qualificationIntroTitle": {
      "type": "string",
      "default": "Fit questions",
      "description": "Short label shown when collecting lead details."
    },
    "contactStepTitle": {
      "type": "string",
      "default": "Your contact details",
      "description": "Heading for the final qualification step."
    },
    "contactStepHelp": {
      "type": "text",
      "description": "One short sentence explaining why contact details are needed."
    },
    "bookingTitle": {
      "type": "string",
      "default": "Choose a time",
      "description": "Heading above the calendar."
    },
    "successTitle": {
      "type": "string",
      "default": "Your strategy call is booked",
      "description": "Heading shown after a successful booking."
    },
    "fallbackTitle": {
      "type": "string",
      "default": "We need more context before booking",
      "description": "Heading shown when a lead is not ready to book."
    },
    "fallbackDescription": {
      "type": "text",
      "description": "Brief next-step message shown below the fallback heading."
    },
    "stepper": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::booking.stepper",
      "required": true
    }
  }
}
